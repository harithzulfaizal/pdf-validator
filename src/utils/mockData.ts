/**
 * Mock data for the PDF validator application
 * This provides a list of sample filenames to use as the masterlist
 */

import srcList from './master_list.json';

// Sample list of PDF filenames that might be in a knowledge base
export const mockMasterList: string[] = srcList;